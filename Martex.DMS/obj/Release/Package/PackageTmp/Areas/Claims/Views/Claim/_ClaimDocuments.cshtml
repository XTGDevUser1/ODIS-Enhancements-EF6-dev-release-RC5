@model int
@{
    var suffixClaimID = Model.ToString();

}
<div class="with-padding">
    <div class="columns">
        <div class="twelve-columns">
            @Html.Editor("Claim_" + suffixClaimID, "DocumentList", new { entityId = Model, entityName = "Claim", documentCategory = "Claim", sourceSystem = "Claim" })
        </div>
    </div>
</div>
